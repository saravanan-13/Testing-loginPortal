create database test;
use test;
create table User (id varchar(20), email varchar(20), name varchar(20));
insert into User (id, email, name) values (1, "cooper@bbt.com", "cooper");

select * from User; 
