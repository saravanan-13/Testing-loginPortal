pipeline {

    agent any
    
    stages {
        stage ('Compile Stage') {
            steps {
                 sh 'cd Registration_Backend'
                 sh 'sudo mvn clean'
               
            }
        }
        
    }
}
