pipeline {

    agent any
    
    stages {
        stage ('Compile Stage') {
            steps {
                 sh 'mvn -f Registration_Backend/pom.xml clean'
               
            }
        }
        
    }
}
